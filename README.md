# Malink releases

This public repository contains immutable Malink Android APK releases and the
official static Malink client/update metadata. The application source is
maintained separately in a private repository.

Install only APKs published under this repository's Releases section. Malink
also verifies each downloaded APK's size, SHA-256 digest, package identity,
version, architecture, and application signing certificate before requesting
installation.
